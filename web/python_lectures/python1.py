# For user input when running code: The "Output" panel is read-only. 
# You must switch to the "Terminal" panel for interactive input.
# To fix this permanently for extensions like Code Runner, enable the setting to run code in the integrated terminal:
# Open Settings (File > Preferences > Settings or Code > Settings on macOS, or press Ctrl+,).
# Search for code-runner.runInTerminal.
# Check the box for "Code Runner: Run In Terminal".
# Restart VS Code
print("Program is starting...")
print(input("Enter number: "))
print("End of program!")